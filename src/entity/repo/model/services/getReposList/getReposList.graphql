query getReposList($first: Int, $after: String, $before: String, $last: Int) {
    search(
        type: REPOSITORY
        query: "is:public sort:interactions"
        first: $first
        after: $after
        before: $before
        last: $last
    ) {
        repositoryCount
        nodes {
            ... on Repository {
                id
                name
                stargazerCount
                url
                defaultBranchRef {
                    target {
                        ... on Commit {
                            history(first: 1) {
                                nodes {
                                    ... on Commit {
                                        committedDate
                                    }
                                }
                            }
                        }
                    }
                }
            }
        }
        pageInfo {
            endCursor
            startCursor
            hasNextPage
        }
    }
}
