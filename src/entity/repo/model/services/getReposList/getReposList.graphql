query getReposList($first: Int, $after: String) {
    search(
        type: REPOSITORY
        query: "is:public sort:interactions"
        first: $first
        after: $after
    ) {
        repositoryCount
        nodes {
            ... on Repository {
                id
                name
                stargazerCount
                url
                defaultBranchRef {
                    target {
                        ... on Commit {
                            history(first: 1) {
                                nodes {
                                    ... on Commit {
                                        committedDate
                                    }
                                }
                            }
                        }
                    }
                }
            }
        }
        pageInfo {
            endCursor
            hasNextPage
        }
    }
}
